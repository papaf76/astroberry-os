name: Astroberry OS builder
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: self-hosted

    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write

    strategy:
      fail-fast: false
      matrix:
        container: ["debian-trixie"]

    steps:
      - name: Login to Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          file: docker/Dockerfile.${{ matrix.container }}
          push: true
          tags: ghcr.io/${{ github.repository }}/${{ matrix.container }}
